name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: OSpoon/workflows/.github/workflows/release.yml@main
    with:
      publish: true
      node-version: 22.14.0
    permissions:
      contents: write
      id-token: write
